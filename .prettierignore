node_modules
dist
build
.next
out
coverage
# contracts/ is a pure Solidity/Foundry project with its own toolchain
# (forge fmt, configured in foundry.toml's [fmt] section) — Prettier has no
# Solidity support installed, and lib/ + .github/ are vendored/generated by
# `forge init`, not hand-authored.
contracts/
pnpm-lock.yaml

# Generator output. Formatting these would make `format:check` pass exactly
# once: drizzle-kit and generate:css re-emit them in their own style, so the
# check would fail again on the next migration or token change, and the diff
# would be noise nobody wrote. tailwind-theme.css says so in its own header.
apps/agent/drizzle/
packages/shared/src/design/generated/

